// SPDX-License-Identifier: GPL-3.0-or-later
// miniaudio implementation TU. miniaudio is a single-header library that
// requires MINIAUDIO_IMPLEMENTATION to be defined in exactly one translation
// unit to emit its definitions. Keep this file minimal — only the define and
// the include.
#define MINIAUDIO_IMPLEMENTATION
#include <miniaudio.h>
